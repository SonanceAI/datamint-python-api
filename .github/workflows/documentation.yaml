name: Deploy Sphinx documentation to Pages

on:
  push:
    branches: [main, feat/test_doc] # branch to trigger deployment


# Cancel any in-progress job or run
concurrency:
  group: gh-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pages:
    runs-on: ubuntu-22.04
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    permissions:
      pages: write
      id-token: write
    steps:
    - id: deployment
      uses: sphinx-notes/pages@v3Z